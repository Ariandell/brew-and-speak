{
  "name": "english-with-coffee-deployment",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix v3 run build",
    "test": "npm --prefix v3 run app:release-check"
  }
}
