import { Welcome } from '../../screens/welcome/Welcome';

/** The intro screen, shown on the real water rather than on a flat colour. */
export const WelcomeEntry = () => <Welcome onStart={() => undefined} />;
